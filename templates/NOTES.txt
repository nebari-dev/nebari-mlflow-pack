{{ include "nebari-mlflow-pack.fullname" . }} has been deployed.

{{- if .Values.nebariapp.enabled }}

NebariApp is ENABLED. The nebari-operator will configure:
  - HTTPRoute for hostname: {{ .Values.nebariapp.hostname }}
  {{- if .Values.nebariapp.auth.enabled }}
  - OIDC authentication via {{ .Values.nebariapp.auth.provider }}
  {{- end }}

Check the NebariApp status:
  kubectl get nebariapp {{ include "nebari-mlflow-pack.fullname" . }} -n {{ .Release.Namespace }}

{{- else }}

NebariApp is DISABLED. To access MLflow locally:

  kubectl port-forward svc/{{ .Release.Name }} 5000:80 -n {{ .Release.Namespace }}

Then open http://localhost:5000 in your browser.

{{- end }}
